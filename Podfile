# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'MetaData' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
	pod 'ID3EDIT', '~> 4.0'

  # Pods for MetaData
  pod 'RealmSwift'

  target 'MetaDataTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
